import Vue from "vue";
/*import mapboxgl from '../_static/_libs/mapboxgl/mapbox-gl-enhance'
import "../_static/_libs/iclient-mapboxgl/iclient-mapboxgl.min"
import mapMixin from '../_mapcore/_sourceModel/index'*/
/*function callHook(vm, hook, ...params) {
  const { options } = vm.constructor;
  options.mixins &&
  options.mixins.forEach(mixin => {
    mixin[hook] && mixin[hook].call(vm, ...params);
  });
  options[hook] && options[hook].call(vm, ...params); // 调用子组件的生命周期
}*/
/*
export default class MapGetter extends Vue {
  data(){
    return {
      map:null,
    }
  }
  methods(){
    _SourceConfig(map){
      return new SourceConfig({map:null})
    }
  }

}*/
